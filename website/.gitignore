node_modules/
.next/
out/
.vercel/
var/
*.log
.env*
# El ejemplo sí va al repositorio: no tiene valores, y es lo que le dice a quien
# despliega qué hay que setear. Sin él, la lista vive en la cabeza de una persona.
!.env.example
.DS_Store
tsconfig.tsbuildinfo

# Salidas generadas por el pipeline de scrapers (no curadas)
data/listings.raw.json
data/listings.matched.json
data/review-queue.json
data/price-history.snapshots.json
data/model-images.json
# Generados por la consola de admin
data/match-decisions.json
data/manual-listings.json
data/manual-models.json
